# ⚔️ Projeto: Seleção de Classes (RPG Medieval)

Este projeto é um site estático focado na **Seleção de Classes** para um jogo fictício de RPG Medieval. O objetivo é apresentar ao jogador as classes disponíveis (Maga, Arqueiro e Elfo) em páginas de detalhes ricas e temáticas.

## 🖥️ Demonstração

Recomenda-se adicionar um screenshot da sua página principal ou das "fichas" de classe aqui para dar uma prévia visual do projeto.

*(Opcional: Adicione um screenshot. No GitHub, você pode simplesmente arrastar e soltar uma imagem no editor de texto do README)*

``

## ✨ Funcionalidades

* **Página Principal (`index.html`):** Uma página de "splash" que apresenta as 3 classes lado a lado para seleção (baseado no seu código original).
* **Páginas de Detalhes:** Páginas dedicadas para cada classe (`maga.html`, `arqueiro.html`, `elfo.html`) com descrições, habilidades e atributos.
* **Design Temático:** Uso de fontes, cores e layout que criam uma atmosfera medieval.
* **Fichas de Classe com CSS:** As páginas de detalhes não usam imagens de pergaminho; elas criam uma "ficha" estilizada puramente com HTML e CSS (bordas, fundos, sombras).
* **HTML Semântico:** Uso correto de tags como `<header>`, `<main>`, `<footer>` e `<article>` para melhor acessibilidade e SEO.

## 🛠️ Tecnologias Utilizadas

* **HTML5** (Semântico)
* **CSS3**
    * Fontes customizadas via `@import` (Google Fonts: `MedievalSharp`)
    * Layouts com Flexbox
    * Gradientes CSS (para o fundo do `body`)
    * Estilização de bordas (`border-image`)
    * Pseudo-elementos (`::before` para ícones nas listas)
* **Google Fonts**

## 🚀 Como Executar

Como este é um projeto puramente estático (HTML/CSS), não há necessidade de instalação ou um servidor complexo.

1.  Clone este repositório (ou baixe os arquivos ZIP):
    ```bash
    git clone [https://github.com/SEU-USUARIO/SEU-REPOSITORIO.git](https://github.com/SEU-USUARIO/SEU-REPOSITORIO.git)
    ```
2.  Navegue até a pasta do projeto:
    ```bash
    cd projeto-rpg
    ```
3.  Abra o arquivo `index.html` (ou qualquer um dos outros arquivos `.html`) diretamente no seu navegador de preferência (Ex: Google Chrome, Firefox).

## 👩‍💻 Autor

* **Hilton Medeiros Amorim**
